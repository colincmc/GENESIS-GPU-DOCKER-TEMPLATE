{
  "name": "genesis-gpu-docker-template",
  "version": "1.0.0",
  "description": "GPU Docker base template — nvidia/cuda + RAPIDS + NeMo + Node.js. Not a running service — a build template. When GPU rental goes live, docker build and go.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.11.5",
    "@types/express": "^4.17.21"
  }
}
